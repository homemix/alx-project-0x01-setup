# Next.js Web Application

## 🧩 Project Overview
This project is a **Next.js** web application built with **TypeScript** and **Tailwind CSS**.  
It demonstrates essential concepts of modern frontend development — including dynamic routing, reusable components, API integration, and state management — while maintaining clean architecture and responsive design.

---

## 🎯 Learning Objectives
By completing this project, you will learn how to:

- Set up a Next.js app with **TypeScript** and **Tailwind CSS**
- Implement **dynamic routing** and client-side navigation
- Build **reusable React components** with strong TypeScript typing
- **Fetch and display data** from external APIs (JSONPlaceholder)
- Use **modals** for adding posts and users interactively
- Understand **static site generation** with `getStaticProps`
- Manage state using **React hooks**
- Organize code with a scalable **project structure**
- Apply **responsive UI design** principles

---

## ⚙️ Requirements

### Technical
- **Node.js** ≥ v16
- **npm** or **yarn**
- **Next.js** (latest)
- **TypeScript**
- **Tailwind CSS**
- **ESLint** (for code linting)

### Functional
#### Base Setup
- Create a Next.js app with TypeScript, Tailwind CSS, and ESLint
- Configure global styles and folder structure

#### Navigation
- Header with links to **Home**, **Posts**, and **Users**
- Seamless navigation without page reloads

#### Posts
- Fetch and display posts from **JSONPlaceholder API**
- Create reusable **PostCard** and **PostModal** components
- Implement post creation and form state management

#### Users
- Fetch and display users from **JSONPlaceholder API**
- Create reusable **UserCard** and **UserModal** components
- Handle nested user data structures

---

## 🚀 Getting Started

```bash
# Clone the repository
git clone https://github.com/yourusername/nextjs-web-app.git

# Navigate into the project directory
cd nextjs-web-app

# Install dependencies
npm install

# Run the development server
npm run dev
